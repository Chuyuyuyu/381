<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Friend List</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/images/4.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Montserrat', sans-serif;
            color: #f8f9fa;
            overflow: hidden;
        }

        .container {
            max-width: 900px;
            background-color: rgba(0, 0, 0, 0.85);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
        }
        
        h2 {
            font-family: 'Bangers', cursive;
            text-align: center;
            color: #ffffff;
            margin-bottom: 20px;
            font-size: 28px;
        }

        .table thead th {
            background-color: #343a40;
            color: #ffffff;
        }
        .table tbody tr {
            background-color: rgba(52, 58, 64, 0.9);
        }
        .table tbody tr:hover {
            background-color: rgba(52, 58, 64, 0.7);
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            border: none;
            font-weight: bold;
        }
        .btn-danger {
            background-color: #dc3545;
            border: none;
        }
        .btn-secondary {
            background-color: #6c757d;
            border: none;
            width: 100%;
            margin-top: 15px;
        }

        .form-group label {
            font-weight: bold;
            color: #dddddd;
        }

        .text-center p {
            color: #ffffff;  
            display: inline-block;  
            padding: 5px 0;  
        }

        a {
            color: #00bfff;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
            color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="mb-4">Your Friend List</h2>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Note</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% friends.forEach(function(friend, index) { %>
                <tr>
                    <td><%= friend.name %></td>
                    <td><%= friend.email %></td>
                    <td><%= friend.note ? friend.note : 'No note available' %></td>
                    <td>
                        <form action="/friends/delete/<%= friend._id %>" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                        <a href="/friends/edit/<%= friend._id %>" class="btn btn-primary btn-sm">Edit</a>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <a href="/dashboard" class="btn btn-secondary mt-4">Back to Dashboard</a>
        <a href="/logout" class="btn btn-danger mt-4">Logout</a>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

